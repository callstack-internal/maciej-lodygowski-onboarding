import {Color} from './color.ts';
import {ViewStyle} from 'react-native';

export const bottomBorder: ViewStyle = {
  borderBottomWidth: 1,
  borderColor: Color.LightGrey,
};

export const paddingStandard: ViewStyle = {
  paddingHorizontal: 8,
  paddingVertical: 12,
};
